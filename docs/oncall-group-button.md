# Oncall 群按钮技术方案

在最终回答反馈旁增加「拉起 Oncall 群」按钮。点击后由 BotMux 调用 BytedOncall 建群，并在原回答下回复群链接。原反馈评价、二级选项及其权限策略保持原有行为。

本文说明当前实现；配置、代码入口、测试和部署步骤见 [开发文档](oncall-group-development.md)。

## 功能范围

| 项目 | 当前行为 |
| --- | --- |
| 开关 | 每个 Bot 独立配置，默认关闭 |
| 生效群 | Dashboard 下拉多选来源群；空列表不生效，不表示全部群 |
| 建群目标 | 每个 Bot 一套租户、问题分类和区域，由管理员配置 |
| 卡片操作 | 直接点击建群，不增加目标或区域选择弹窗 |
| 问题归并 | 同一 Bot、同一来源群、同一问题复用群；不同问题分别创建 |
| 原反馈 | 仍支持评价和二级选项；关闭反馈也可单独展示 Oncall 按钮 |
| 建群结果 | 单独回复链接或错误提示，不修改原按钮状态 |

Dashboard 入口是「消息卡片 / 最终回答反馈」旁的「支持拉起 Oncall 群」和「生效群」。这里选择的是**按钮出现在哪些群**，不是选择一个已有 Oncall 群，也不是让所有问题都进入同一个固定群。

普通最终回答和 `botmux send --response-kind final` 复用同一装配函数。私聊和过程消息不追加按钮；CLI 发送中的自定义卡片、语音、独立顶层发送、指定其它投递位置及会议托管发送也不追加。

## 配置边界

接入需要三类配置，不需要给每个点击者配置凭据：

- **服务凭据**：一个 `ONCALL_SERVICE_SECRET`。管理员可在 Oncall 设置旁填写 Service Secret，保存到宿主 `~/.botmux/.env`，重启 daemon 后生效；也可由部署环境注入。使用相同环境的 Bot 共用该服务账号。
- **建群目标**：数据目录中的 `oncall-group-targets.json`，按 Bot App ID 保存五项参数。
- **按钮范围**：Bot 配置中的 `oncallGroup.enabled` 和 `oncallGroup.chatIds`，由 Dashboard 保存并热更新。

Secret 不进入普通 Bot 配置、目标 JSON、卡片或会话 CLI 环境。目标文件由管理员维护，不能由卡片点击参数指定。具体格式见 [开发文档](oncall-group-development.md)。

凭据页面通过 `GET /api/oncall-service-secret` 读取已保存配置的存在状态，通过 `PUT` 提交新凭据。两者均要求宿主管理权限；写请求复用同源和 CSRF 校验。接口不返回密钥，保存采用现有宿主安全文件读写与文件锁，保留其他 `.env` 配置。输入框不回填原值，保存结束后清空；空值不能覆盖原凭据。状态仅表示文件中是否已配置，不代表运行中的 daemon 已重启或平台授权有效。

## 鉴权与身份

服务账号回答“哪个程序有权调用接口”，点击者回答“这次问题是谁发起的”。两者分开传递：

```http
Authorization: Bearer <Service Secret>
x-api-user: <点击者的平台用户名>
```

`endpoint` 必须是支持上述服务账号 Bearer 鉴权的 BytedOncall 官方 HTTPS 网关。客户端直接请求该网关，不依赖 Mesh/DECC；不自行换取、缓存或刷新 JWT。配置项接收 Service Secret，不接收个人 JWT，也不实现 AK/SK 签名。Secret 轮换后需更新 daemon 环境并重启。

点击者身份取自飞书回调的 `operator.open_id`，通过现有身份缓存和通讯录解析邮箱，再校验 `emailDomain`，取邮箱本地部分作为 `x-api-user`。不取原提问者、会话 owner 或运行机器登录用户，也不调用个人 JWT 获取逻辑。

按钮范围不继承反馈的 `audience` 设置。当前回调允许所选来源群中、能通过点击者身份和邮箱校验的用户发起请求；不额外限制为原提问者。

服务账号必须具有建群接口和目标租户的权限，部署前应确认邮箱到平台用户名的映射及平台代建规则。BotMux 当前没有额外调用“添加群成员”接口；**传入点击者用户名不等于已验证平台自动拉人入群**，真实验收需检查工单发起人和群成员。其他人再次点击已成功的同一问题时仅得到已有链接，不额外发起邀请。

## 调用流程

1. 发送最终回答时按 Bot 开关和来源群装配按钮。成功发送后保存卡片 ID、问题 ID、来源群和回答文本；索引失败只记警告，不重发回答。
2. 点击触发 `oncall_group_create`。回调查来源记录，再检查当前群范围、回调群和点击者身份；关闭开关后旧卡片也不能继续建群。
3. 查询已有建群记录。成功则复用链接；处理中或结果不明则提示核对；仅无记录或明确失败时继续。
4. 加载目标和 Secret，解析点击者邮箱，读取原问题。准备失败时不占用建群记录，也不调用平台。
5. 以文件锁认领请求并写入 `pending`，调用一次平台接口，保存结果。失败响应经过固定文案处理，不向用户透传平台响应正文。
6. 在原回答下回复群链接或错误。成功结果落盘后，即使这次回复失败，下一次点击也只补发链接。

## 接口约定

请求方法为 `POST`，路径为 `/api/v1/oncall_platform/api/inf/v1/chat/`，主机由部署配置提供。客户端使用 20 秒超时，拒绝重定向，不自动重试建群请求。

| 字段 | 来源或固定值 |
| --- | --- |
| `tenant_id`、`type_id`、`region` | 对应 Bot 的建群目标 |
| `type` | `create_chat` |
| `priority` | `P2` |
| `source_type` | `open_api` |
| `source_location` | `botmux` |
| `trigger_message` | 原问题、最终回答、来源群链接、原消息 ID |

当前代码分别用 `slice` 截取问题前 8,000、回答前 16,000 个 JavaScript 字符串单元，再附来源信息。成功要求响应 `code === 0`，且包含 `data.oncall_flow_id` 和格式合法的 `data.open_chat_id`。只保存这两个结果字段；接口可能返回的 `chat_id` 当前不使用。

## 去重与状态

数据保存在 daemon 数据目录下的 `oncall-groups/`，与反馈数据库独立。`messages/` 按 Bot App ID 和卡片消息 ID 建索引；`requests/` 按 Bot App ID、来源群 ID、问题 ID 建索引。文件名使用上述字段的 SHA-256，记录以文件锁和原子写入保护，文件权限为 `0600`。

| 状态 | 含义 | 再次点击 |
| --- | --- | --- |
| 无记录 | 尚未调用平台 | 校验通过后认领并提交 |
| `pending` | 已认领；也可能在调用期间退出 | 不再次提交，提示核对 |
| `succeeded` | 已保存流程 ID 和群 ID | 返回已有群链接 |
| `failed` | 收到明确拒绝响应 | 允许用户手动重试 |
| `unknown` | 可能已创建，无法确认 | 不再次提交，提示核对 |

HTTP `400/401/403/404/422/429` 归为明确失败，其中 `401` 提示更新凭据，`403` 提示检查接口或租户权限。其它非成功 HTTP 响应、超时、网络异常、不可解析响应及不符合成功结构的响应归为结果不明；HTTP 成功但业务 `code` 非零也保守处理为结果不明。

去重依赖这份持久化数据，不是平台侧幂等键。独立部署如果使用不同数据目录，不共享去重记录；删除记录、丢失数据或盲目重置 `pending/unknown` 都可能造成重复建群。当前没有自动清理或恢复这些记录的管理入口。

## 验证边界

当前实现只增加上述按钮、配置、回调及独立建群记录；不修改全局 CLI 身份注入，不把建群结果写进反馈评价，也不新增认证 SDK 或 adapter。原反馈重渲染时通过统一按钮工厂恢复 Oncall 回调定义，避免二级反馈操作后按钮失效。

自动化测试使用受控 `fetch` 和临时数据目录，不提供运行时 mock 接口，不创建真实群。服务账号代建、点击者实际入群及部署账号权限，必须另行完成真实飞书和平台联调，不能用单测通过代替。
